#include "monty.h"

/**
 * _nop - Does nothing.
 * @head: Pointer to the head of the stack (unused in this function).
 * @line_num: Line number in the Monty file (unused in this function).
 */

void _nop(stack_t **head, unsigned int line_num)
{
	(void) line_num;
	(void) head;
}
